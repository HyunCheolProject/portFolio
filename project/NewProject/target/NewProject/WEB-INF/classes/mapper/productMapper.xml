<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="Product">
    <insert id="save" parameterType="product">
        insert into products_table (productName, productWriter, productPw, productContents, productPrice, productCreatedDate, productFileName)
        values (#{productName}, #{productWriter}, #{productPw}, #{productContents}, #{productPrice}, now(), #{productFileName})
    </insert>

    <select id="findAll" resultType="product">
        select * from products_table order by  id desc
    </select>

    <select id="findById" parameterType="Long" resultType="product">
        select * from products_table where id=#{id}
    </select>

</mapper>














