<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="Order">
    <insert id="save" parameterType="order">
        insert into order_table (orderName, postcode, address, detailAddress, extraAddress, requests, orderMobile, orderProduct, orderAmount, orderPrice, cartId)
        values (#{orderName}, #{postcode}, #{address}, #{detailAddress}, #{extraAddress}, #{requests}, #{orderMobile}, #{orderProduct}, #{orderAmount}, #{orderPrice}, #{cartId})
    </insert>

<!--    <select id="findAll" parameterType="Long" resultType="order">-->
<!--        select * from order_table where cartId=#{cartId}-->
<!--    </select>-->

    <select id="findById" parameterType="Long" resultType="order">
        select * from order_table where id=#{id};
    </select>

</mapper>














